<table width = 1200>
  <tr>
    <th>Código</th>
    <th>Cliente</th>
    <th>Email</th>
    <th>Categoria</th>
    <th>Status</th>
  </tr>

  <% if @rentals.any? %>
  <% @rentals.each do |rental| %>
  <tr>
    <td align = CENTER><%= link_to rental.code, rental_path(rental) %></td>
    <td align = CENTER><%= rental.client.name %></td>
    <td align = CENTER><%= rental.client.email %></td>
    <td align = CENTER><%= rental.car_category.name %></td>
    <td align = CENTER><%= rental.status %></td>
  </tr>
  <% end %>
  <% end %>
</table>
