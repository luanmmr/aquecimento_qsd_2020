<h1> Efetivar Locação </h1>

<% if @car_rental.errors.any? %>
<h3> <%= pluralize(@car_rental.errors.count, 'falha') %> </h3>
<p>Você deve corrigir os seguintes erros para continuar:</p>

<ul>
  <% @car_rental.errors.values.each do |error_messages_group| %>
  <% error_messages_group.each do |error_message| %>
  <li><%= error_message %></li>
  <% end %>
  <% end %>
</ul>
<% end %>


<% if @cars.any? %>
<%= form_for @car_rental, url: reserve_rental_path do |f| %>
<%= f.label :car_id, 'Carros disponíveis' %>
<%= f.collection_select :car_id, @cars, :id, :full_description %>
<%= f.submit 'Efetivar' %>
<% end %>

<% else %>
<p> Nenhum carro para essa categoria está disponível </p>

<% end %>
